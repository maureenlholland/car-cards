// Home page
// Redirect to login if user not identified
// Show add new deck button if user has no decks (like attribute, just a label field)
// Redirect to only deck if user has 1 deck
// Show list of decks if user has more than 1 deck

import React from 'react';

export default function Home() {
    return <div>Home stub</div>
}
